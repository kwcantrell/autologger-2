// Placeholder shell (task 1.1 — workspace scaffold). Later phases replace this
// with the L0/L1/L2 drill-down site rendered from the generated `atlas.json`
// (see design.md D1). Kept deliberately minimal until the extraction pipeline
// (phase 3+) has something real to render.
export function App() {
  return (
    <main>
      <h1>AutoLogger — Architecture Docs</h1>
      <p>Placeholder page. The extraction pipeline and drill-down site land in later phases.</p>
    </main>
  );
}
