# event-generate-hardening — tasks

> PROVISIONAL until the adversarial panel + gate pass (opsx:propose ordering rule).
> file:line anchors are orientation only — locate code by content before editing.

## 1. Server: body bounds + envelope field

- [ ] 1.1 `eventGenerateBodySchema` (server/src/schemas.ts): add `.max(500)` to the
      selection array, `.max(200)` to `category_id`, `.max(200)` to `option_label`
      (nullable stays). Unit tests: each bound's accept/reject edge (500/501
      entries; 200/201-char id; 200/201-char label) — rejection surfaces as the
      existing malformed 400 via the route (int test asserts one bound case
      end-to-end, before any delete/spawn).
- [ ] 1.2 Event store: add `hasAutoGeneratedEvents(): boolean` (EXISTS query, SQL
      predicate) and thread `has_auto_generated` into the events list envelope
      (route GET handler). Unit tests: SQL-predicate edge cases
      (true/1/"true"/malformed metadata), whole-session semantics (auto row
      outside the page window still reports true), and the **JS↔SQL
      predicate-parity test** (gate ruling E3: the route's JS `isAuto` predicate
      and the store's SQL predicate agree on the same metadata corpus). Int
      test: `GET …/events?limit=1` on a session whose auto row is
      beyond the window → `has_auto_generated: true`.
- [ ] 1.3 Conformance: regenerate the captured events-list fixture(s) and update the
      hand-written server-side response construction + any shape tests
      (`web-api-response-conformance` two-sided check stays green).

## 2. Server: delete-after-success regenerate

- [ ] 2.1 Event store + hub: add `deleteEventsByIds(ids)` — inTxn,
      **chunked ≤500 ids per DELETE statement inside the one transaction** (the
      better-sqlite3 bind ceiling is 32,766; an unbounded snapshot must not 500 a
      successful run), one `event.changed` broadcast iff total deleted > 0,
      returns count; unit test the chunk boundary (e.g. 1,001 ids → one
      transaction, one broadcast, all rows gone). Remove `deleteAutoGeneratedEvents`
      and migrate its unit tests to the new pair (characterization first: pin the
      new RPC's broadcast/transaction behavior like the old one's tests did).
- [ ] 2.2 Route rework (events.ts generate handler): on regenerate — derive the
      snapshot ids **route-side** from the prologue's existing
      `hub.exportEvents()` read via the route's JS auto-predicate (gate ruling
      E3; one read feeds snapshot + exclusion by construction), in the
      synchronous prologue (after slot acquire, before spawn); exclude the
      snapshot from `existingEventsForGenerate` AND from the anchor basis
      (`timecodeWallAnchors` feed — thread the snapshot into the run context so
      `create_event`'s anchor computation filters it); delete the snapshot ids
      only when `outcome.ok && createdEvents > 0` (gate ruling E1), before
      building the 200, **via a freshly re-acquired
      `getSessionHub(c, sessionId)` — never the prologue's hub reference** (idle
      eviction can close it mid-run); `deleted` = the delete's actual count
      (`0` on zero-created success); 502
      path deletes nothing. Non-regenerate paths byte-identical.
- [ ] 2.3 Int tests (events.generate.int.test.ts, fake-CLI harness): failed run
      preserves prior auto rows (and keeps partial inserts); successful run
      deletes exactly the pre-run snapshot (a non-auto row and the run's own new
      rows survive); prompt enumeration excludes old auto rows on regenerate
      (fake CLI records the message); `deleted` reflects a mid-run manual delete
      of a snapshot row; **zero-created success keeps the prior set** (fake CLI
      that calls no tools → 200 `{created: 0, cap_hit: false, deleted: 0}`,
      rows intact — gate ruling E1); pre-guard 400s still delete nothing (re-pin
      the ordering tests from delete-before to delete-after).

## 3. Web: truthful Regenerate label

- [ ] 3.1 `web/src/api/types.ts`: `EventsResponse.has_auto_generated: boolean` —
      REQUIRED, not optional (an optional field defeats the conformance forcing
      function); re-capture web conformance fixtures.
- [ ] 3.2 `EventLogSheet`: label logic reads `data.has_auto_generated` from the
      sheet's own events query, defaulting to Generate All while the response is
      unavailable (spec "Loading state defaults to the non-destructive label");
      drop the row-scan of the workspace-wide query (KEEP that subscription — the
      timeline-reveal growth lookup uses it). Update the regenerate confirm-dialog
      copy to the delete-after-success truth (rows are replaced only after a
      successful run; mid-run edits to auto rows will be lost). Tests: beyond-page
      truth (auto row absent from returned rows, flag true → Regenerate All),
      loading-state label, and re-pin the existing regen-flag tests to the new
      source.

## 4. Docs + final gates

- [ ] 4.1 README endpoint table: events row gains `has_auto_generated` mention;
      events/generate row's regenerate parenthetical updated to
      delete-after-success wording.
- [ ] 4.2 Final gates: `npm run typecheck`, `npm test`, `npm run e2e`,
      `npm run e2e:visual` (no UI pixels should change — label text is
      data-driven; any visual diff is a defect, not a re-bless).
