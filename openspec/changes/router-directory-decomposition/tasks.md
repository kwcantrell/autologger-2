# router-directory-decomposition — Tasks

> **Gated 2026-08-07.** Fact-check pass (33 claims: 32 confirmed / 1 corrected / 9
> un-vouched), 4-reviewer adversarial panel, and gate rulings E1–E4 have run; their
> outcomes are folded across all four artifacts and recorded in `design.md`'s Panel &
> review log. This is the plan of record.

Phase gates: every phase ends green on `npm run typecheck` **and** `npm test`. Mechanical
moves and import rewrites land as their own commits, separate from semantic edits.
`file:line` anchors are orientation only — **locate the quoted code by content before
editing**.

Per-phase review tiering (gate ruling E4): **Phase 2** gets the full-tier review subagent —
it rewrites imports in four modules serving frozen endpoints, repaths namespace-spy
integration tests, and creates the Hono-freedom invariant that would otherwise fail
silently. Phases 3–5 defer to the whole-branch layered audit.

## 1. Branch and plan of record

- [x] 1.1 Cut a plain branch off `main` (`git checkout -b router-directory-decomposition`; **never** a git worktree — declared repo isolation preference). Verify the working tree is clean and `main` is at the archived step-2 head.
- [x] 1.2 Commit the four gated OpenSpec artifacts as the branch's **first commit**, version-pinning the plan of record before any dispatch. Verify `openspec validate router-directory-decomposition --strict` passes.

## 2. AI-runtime extraction (move + rewrites + enforcement + atlas)

- [x] 2.1 Re-derive the moving inventory against the live tree before moving anything: every production module under `server/src/routers/` that neither constructs a `Hono` instance nor registers a route on one, and every `*.test.ts` there whose subject is one of them. Confirm the expected sets — **11 production modules** (`aiMcpServer`, `aiV2SdkSpawn`, `aiChatRunner`, `aiV2PendingQuestions`, `aiTurnOrchestrator`, `aiTurn`, `aiChatRelay`, `topicGenerate`, `aiChatRegistry`, `eventGeneratePrompt`, `processGroupKill`) and **13 unit test files**. Note `sessionWs.ts` registers a route via `app.get(...)` and **stays**. Record any discrepancy in the ledger rather than silently adjusting.
- [x] 2.2 `git mv` all 24 files to `server/src/ai-runtime/`, preserving filenames (D1) and including `processGroupKill.ts` (D2). Verify `git status` shows renames, not delete+add, and that nothing outside the inventory moved.
- [x] 2.3 Rewrite the **eight** staying files' cluster imports (`./<mod>` → `../ai-runtime/<mod>`): route modules `ai.ts`, `aiV2.ts`, `events.ts`, `transcribe.ts` **and** integration tests `ai.int.test.ts`, `aiV2.int.test.ts`, `events.generate.int.test.ts`, `transcribe.int.test.ts`. Then verify: (a) no `server/src/routers/*.ts` still names a moved module by a `./` specifier; (b) **no `../`-prefixed specifier inside the moved files was edited** (the move is depth-preserving — an edited `../env`/`../aiV2/mcpTools` is a defect); (c) **every `import.meta.url`-relative path literal in the moved files resolves to the same target as before** — grep the moved set for `import.meta.url`/`fileURLToPath`/`__dirname` and check each (seven exist: six `../test/fixtures/*.mjs` and `aiMcpServer.test.ts`'s self-sibling source pin); (d) each namespace-spy integration test and its route module resolve the **identical** specifier so `vi.spyOn` still observes the live module (`aiTurnModule`, `aiV2SdkSpawnModule`, `topicGenerateModule`), and `aiV2.int.test.ts`'s documented import-order property is re-read for continued truth.
- [x] 2.4 Add `'ai-runtime'` to `SERVER_SRC_LAYER_DIRS` in `server/src/packageBoundaries.repo.test.ts` in this same commit. Verify the guard passes, the graph stays acyclic, and the edges **incident to `ai-runtime`** are exactly `routers → ai-runtime` and `ai-runtime → aiV2`, with no `ai-runtime → routers`. **`routers → aiV2` survives** via `routers/aiV2.ts`'s own `../aiV2/mcpTools` import, and all other pre-existing edges are unchanged — do not "fix" them.
- [x] 2.5 Add the three architecture checks to `packageBoundaries.repo.test.ts` (D6), extending it and reusing `walkTsFiles`/`extractImportSpecifiers` — never a parallel walker: (1) no production file under `server/src/ai-runtime/` imports `hono`, `hono/*`, `appEnv`, or anything under `server/src/routers/`; (2) every production module directly under `server/src/routers/` imports `hono`/`hono/*` or `appEnv`; (3) `SERVER_SRC_LAYER_DIRS` matches the production directories present under `server/src` (modulo a named exemption list carrying `test/`), and every enumerated directory is non-empty. All three must be green on the clean tree at this commit.
- [x] 2.6 Demonstrate the negative cases for checks (1) and (2) once in the working tree (a `hono` import added to a runtime module; a non-HTTP module placed under `routers/`), confirm each fails, revert, and record in `.apply/ledger.md`. Ship no permanent violation fixtures.
- [x] 2.7 Atlas, in one unit with the move (D8): add the `ai-runtime` component (`server/src/ai-runtime/**`) to `web-docs/model/components.ts`; **change the `routers-to-claude-cli` relationship's `from` to the new component** (re-anchoring its evidence file alone is insufficient — `checkRelationshipEvidence` never checks component membership, so that would pass green while the atlas still claims `routers` spawns the CLI), and settle the id-rename Open Question; re-attribute the AI-behaviour capability entries. Regenerate the committed snapshot **`web-docs/model/edges.snapshot.json`** via `npm run snapshot -w web-docs` (**not** `atlas.json`, which is git-ignored) **in this same unit, before** asserting the gate. Then verify `npm run docs:check` passes.
- [x] 2.8 Review the regenerated snapshot **per edge with attribution** — every vanished and new edge traced to a decision in the design. Expect more than the two directory edges: `ai-runtime → {session-core, contract, domain}`, `test`-kind edges, and `routers → ai-runtime` in both kinds; no `routers → X` package edge should vanish. Record the attribution in the ledger; an unattributed edge delta blocks the phase.
- [x] 2.9 Re-point the **eight** prose comments citing a moving module's current path: `server/src/aiV2/mcpTools.ts`, `server/src/node/ytdlp.ts` (×4), `packages/contract/src/schemas.ts`, `server/src/routers/aiV2.ts` (a *staying* file), and `server/src/ai-runtime/aiV2SdkSpawn.test.ts` (a *moving* file — `git mv` does not fix it). These are pointers to live modules, **not** past-tense origin headers: update the path, do not strip the comment, and do not convert provenance headers elsewhere in those files. **One is a semantic rewrite, not a path rewrite** — `ytdlp.ts`'s comment asserts "`routers/aiChatRunner.ts` is router-layer" as the justification for a deliberate duplication, and this change falsifies that sentence; restate the rationale rather than just the path. Also refresh `web-docs/model/diagramValidity.ts`'s rationale characterizing `server/src/routers/` as a flat 64-node directory. **Caution:** the boundary test's widened clause regex is a raw-text scan — never write a comment in a `server/src` production file that quotes a forbidden import/export form, or it will red the gate.
- [x] 2.10 Phase gate: `npm run typecheck` + `npm test` + `npm run lint` + `npm run docs:check` green. Verify the baseline `package-architecture` singleton requirement still holds (`aiChatTurns`, the MCP listener singleton, `aiV2PendingQuestions` each resolve from exactly one module). Record in the ledger whether `topicGenerate.real.test.ts` and `eventGenerate.real.test.ts` **ran or skipped** (they are configuration-gated; vitest still collects them, so their imports are runtime-verified either way).

## 3. App-level error class and TimecodeCtx consolidation

- [x] 3.1 Create `server/src/httpError.ts` holding `ApiError` verbatim (D3), delete the declaration from `server/src/routers/_helpers.ts` with **no re-export shim**, and rewrite every importer (`app.ts`, the 10 routers that throw it, and the 3 test files). Verify by grep that no import of `ApiError` resolves into `server/src/routers/`, and that both `instanceof ApiError` sites (`app.onError` and `routers/sessions.ts`) are rewritten in this same commit — a same-commit move with no shim window is what preserves single-module identity.
- [x] 3.2 **Add atlas coverage for the new root-level file in this same phase**: `web-docs/model/components.ts` matches root `server/src` files by **exact-path glob lists**, not a wildcard, so `httpError.ts` would otherwise be a coverage orphan plus an unmapped-import error that hard-fails `docs:check` — while root `npm test` stays green, hiding it for two phases. Extend the appropriate component's glob (or introduce a `server/src/*.ts` glob and re-verify no overlap), and regenerate the snapshot if the edge projection changes.
- [x] 3.3 Drop the duplicate `interface TimecodeCtx` from `routers/_helpers.ts` and import the type from `@autologger/session-core` for the `timecodeCtx(row: Row)` return annotation (D4). **Add no re-export** — nothing imports `TimecodeCtx` from `_helpers`, so a re-export would be a permanent shim serving nobody. Verify exactly one declaration remains repo-wide and `tsc --noEmit` accepts every consumer unchanged.
- [x] 3.4 Phase gate: `npm run typecheck` + `npm test` + **`npm run docs:check`** green. Confirm the `422`/`400`/`416`/dashboard-`422` integration pins and the API-response fixture conformance chain pass unchanged, with no fixture regenerated with different content.

## 4. Interface-only check hardening

- [x] 4.1 Before editing, run the *strengthened* matching logic against the current tree as a throwaway probe and record the result. The panel measured zero violations and zero wildcard clauses across four matcher configurations, so this is expected to find nothing; a surprise is a finding to fix in this phase, never a reason to weaken the check.
- [x] 4.2 Harden `checkInterfaceOnlyConsumption` (D7): match `CONCRETE_PACKAGE_SPECIFIERS` by **specifier prefix** (`spec === pkg || spec.startsWith(pkg + '/')`); widen `IMPORT_CLAUSE_RE` from `\bimport\b` to `\b(?:import|export)\b` — **use a non-capturing group**, since a capturing group shifts the match indices so the specifier test reads clause text and the check silently always passes; and reject **wildcard clauses** (`import * as …` / `export * from …`) against those packages outright. Leave the general `extractImportSpecifiers` unchanged — read it, confirm it already matches both keywords and serves the other four checks, and record that verification (fixing an already-closed hole is its own defect).
- [x] 4.3 Give the check the **synthetic-tree mutation coverage it has never had** — it is the only check in that file without any, and this phase reshapes it. Using the file's existing synthetic-tree pattern (the function is already parameterized over `repoRoot`, so no permanent fixture enters the real tree): clean tree → no violation; bare, deep-subpath, `export … from`, and wildcard forms → violation each; facade imports (`SessionHubFacade`) and `export type` clauses → no violation.
- [x] 4.4 Demonstrate the deep-subpath, `export … from`, and wildcard negative cases once in the working tree against a real `server/src` production file, confirm each fails, revert, and record in `.apply/ledger.md`.
- [x] 4.5 Phase gate: `npm run typecheck` + `npm test` green with the boundary test passing on the clean tree.

## 5. Docs, final gates, and audit package

- [x] 5.1 Update `README.md` and `CLAUDE.md`'s Source layout: the new `server/src/ai-runtime/` block in the annotated tree, `httpError.ts` at app root, and the `_helpers.ts` line (which currently advertises `ApiError`). Verify the README **endpoint table is unchanged** — every module it names stays. Also correct `server/src/routers/aiV2.int.test.ts`'s header comment: the phase-2 review found two of its factual claims about `attemptDesignTurnSpawn` (the call-site claim and the import-order-property rationale) were already false on `main` before this branch — legitimate carried work, disclosed in the commit body and `.apply/ledger.md`, landing here since it is a comment-only correction alongside this task's other doc edits.
- [x] 5.2 Full gates: `npm run typecheck`, `npm test`, `npm run lint`, `npm run docs:check`, `npm run e2e` (chromium + login-gate projects) and `npm run e2e:visual`. **Baseline provenance:** the visual snapshots were regenerated wholesale on **2026-08-07** by `d23e8b9` (44 PNGs) — not 2026-07-14 as an earlier draft and the repo's own task-authoring rule state. Because that currency is asserted rather than known, capture a **pre-branch visual run on `main`** as the comparison point; against that baseline this change touches no `web/` source, so any diff is branch-induced signal to investigate, never to re-bless.
- [x] 5.3 If `main` moved during the apply, re-derive task 2.1's inventory, regenerate the snapshot, and re-run `docs:check` — the three most drift-exposed artifacts (`edges.snapshot.json`, `components.ts`, `packageBoundaries.repo.test.ts`) all merge textually while going semantically stale.
- [x] 5.4 Assemble the whole-branch layered audit package per CLAUDE.md: contract/seam-relevant diffs of the surface-touching phase, full diffs of the deferred phases (3–5), diffs of clean phases sharing files or state with deferred ones, re-reads of any phase modified after its review closed, the branch's materialized file list (`git diff --stat` / `git log --stat` + stray-file scan), tree hygiene in affirmative-evidence form, and all call sites of every declared seam checked against its declared property. **Scope the package from the corrected eight-file edit set** — the four staying `*.int.test.ts` are modified, not untouched.
- [x] 5.5 Run the whole-branch review over that package and land findings as fix waves (re-reviews scope to the fix diff, not a cumulative re-read).
